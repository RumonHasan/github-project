import React from 'react';
import { TextField } from '@mui/material';

const SearchBar = (props) => {
  const {githubUser, setGithubUser} = props;
  return (
    <div>SearchBar</div>
  )
};
export default SearchBar;
